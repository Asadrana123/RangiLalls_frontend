// Create a script that generates a dynamic sitemap
import fs from 'fs';
import axios from 'axios';

async function generateSitemap() {
  // Fetch all properties from your API
  const properties = await axios.get('https://rangilalls-backend.onrender.com/api/properties');
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://www.rangilalls.com/</loc>
    <lastmod>${new Date().toISOString().split('T')[0]}</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://www.rangilalls.com/about</loc>
    <lastmod>${new Date().toISOString().split('T')[0]}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  ${properties.data.map(property => `
  <url>
    <loc>https://www.rangilalls.com/property/${property._id}</loc>
    <lastmod>${new Date(property.updatedAt).toISOString().split('T')[0]}</lastmod>
    <changefreq>daily</changefreq>
    <priority>0.8</priority>
  </url>
  `).join('')}
</urlset>`;

  fs.writeFileSync('./public/sitemap.xml', sitemap);
}

generateSitemap();